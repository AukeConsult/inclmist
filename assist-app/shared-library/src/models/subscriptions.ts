export interface QuerySubscription {
    profileId: string
    queryId: string
    queryName: string
    queryImage?: any
    profileImage?: any
    dailyCost: number
}

export interface profileSubscription {
    profileId: string
    profileName: string
    profileImage?: any
    dailyCost: number
}