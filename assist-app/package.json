{
  "name": "main",
  "version": "1.0.0",
  "scripts": {
    "build-all": "cd shared-library & npm run build & cd ../shared-backend & npm install & npm run build & cd ../gpt-chat & npm install"
  }
}
