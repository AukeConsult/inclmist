export interface QuerySubscription {
    pid: string
    qid: string
    displayName: string
    imageUrl?: string
    image?: any
    dailyCost: number
}

export interface profileSubscription {
    pid: string
    displayName?: string
    imageUrl?: string
    image?: any
    dailyCost: number
}